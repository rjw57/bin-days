# Bin Days system planning

## Diagrams

View diagrams via:

```console
$ docker run -it --rm -p 8080:8080 -v $PWD/structurizr-workspace:/usr/local/structurizr structurizr/lite
```

and then on http://localhost:8080/
